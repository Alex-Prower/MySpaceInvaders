//Коллизия
float rightCalibration = e.getPosition().x + e.getGlobalBounds().size.x;
//Право
if (this->Left0Right1 == 0) {
    if (e.getPosition().x < 0.f) 
    {
        this->Left0Right1 = 1; 
        break;
    }
    e.move({ -2.f * this->enemySpeed, 0.f});
}
if (this->Left0Right1 == 1) {
    if (rightCalibration >= 640.f) {
        this->Left0Right1 = 0;
        std::cout << "This position: " << rightCalibration << "\n";
        break;
    }
    e.move({ 2.f * this->enemySpeed, 0.f} );